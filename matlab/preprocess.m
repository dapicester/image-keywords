% PREPROCESS: build vocabulary and compute histograms

setup

%% Get images from database

% 1. connect to database
% 2. query for category keywords and save image names into text files
% 3. retrieve images into data folder

%% Compute visual word vocabulary

% 1. load text files with image names
% 2.1. compute vocabulary from subset of images and save to disk
% 2.2. load vocabulary from disk if already computed

%% Compute histograms

% 1. compute histograms from image lists and save to disk
